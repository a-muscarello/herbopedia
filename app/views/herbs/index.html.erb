
<p id="notice"><%= notice %></p>

<h1>Herbs</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @herbs.each do |herb| %>
      <tr>
        <td><%= herb.user_id %></td>
        <td><%= link_to 'Show', herb %></td>
        
        <td><%= link_to 'Index', herb %></td>

      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Herb', new_herb_path %>
