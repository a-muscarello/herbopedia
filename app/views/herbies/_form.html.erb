<%= form_with(model: herby, local: true) do |form| %>
  <% if herby.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(herby.errors.count, "error") %> prohibited this herby from being saved:</h2>

      <ul>
      <% herby.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_name %>
    <%= form.text_field :user_name %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
